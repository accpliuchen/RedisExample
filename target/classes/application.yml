server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/redis_app
    username: root
    password: Liuchen198327!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

redis:
  host: localhost
  port: 6379
